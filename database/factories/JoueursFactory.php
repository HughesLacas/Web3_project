<?php

/* @var $factory \Illuminate\Database\Eloquent\Factory */

use App\Joueur;
use App\User;
use Faker\Generator as Faker;

$factory->define(Joueur::class, function (Faker $faker) {

        /*return [
            'prenom' => $faker->firstNameName,
            'nom' => $faker->lastName,
            'reseau'=>default(false),

        ];*/
});
